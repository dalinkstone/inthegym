package main 

import (
	"fmt"
)

func binarysearch(nums array, target int) bool {
	int l, r = 0, len(nums)

	for l < r {
		mid = (l + r) / len(nums)

		if nums[mid] == target {
			return true
		}

	}
}

func main() int {

}
